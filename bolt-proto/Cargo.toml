[package]
name = "bolt-proto"
version = "0.1.0"
authors = ["Luc Street <lucis-fluxum@users.noreply.github.com>"]
edition = "2018"

[dependencies]
bolt-proto-derive = { path = "../bolt-proto-derive" }

bytes = "0.5.3"
failure = "0.1.6"
tokio = { version = "0.2", features = ["full"] }
