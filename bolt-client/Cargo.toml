[package]
name = "bolt-client"
version = "0.1.0"
authors = ["Luc Street <lucis-fluxum@users.noreply.github.com>"]
edition = "2018"

[dependencies]
bolt-proto = { path = "../bolt-proto" }

bytes = "0.5.3"
failure = "0.1.6"
tokio = { version = "0.2", features = ["full"] }
